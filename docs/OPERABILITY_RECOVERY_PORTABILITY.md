# Operability, Recovery and Portability

The operations foundation exposes deterministic watcher, connector/model health,
cost, recovery and portability receipts.

Watchers notify or propose only. They do not mutate sovereign state. Derived Mind
state is rebuildable; the recovery drill explicitly asserts no canonical data loss.
Provider portability is represented by a provider-neutral export receipt and never
contains secrets.

